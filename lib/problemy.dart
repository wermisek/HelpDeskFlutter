// problemy.dart
import 'package:flutter/material.dart';

class ProblemyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Moje Zgłoszenia'),
      ),
      body: Center(
        child: Text('Tutaj będą wyświetlane wszystkie Twoje zgłoszenia.'),
      ),
    );
  }
}
