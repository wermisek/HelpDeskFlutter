name: HelpDeskDrzewniak
description: "Helpdesk application made in flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  url_launcher: ^6.0.9
  dropdown_button2: ^2.0.1
  window_manager: ^0.4.3
  shelf: ^1.2.0
  shelf_router: ^1.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: any

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

